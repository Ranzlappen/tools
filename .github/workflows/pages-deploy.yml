name: pages-deploy

on:
  push:
    branches: [main]
    # Skip rebuilds when only Vercel-only paths change. Vercel handles
    # those out-of-band.
    paths-ignore:
      - 'api/**'
      - 'vercel.json'
      - '.vercelignore'
      - '.github/**'
      - '**/*.md'
      - 'CHANGELOG.md'
      - 'LICENSE'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Configure Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
        with:
          # If Pages isn't enabled yet, try to enable it automatically.
          # Requires the `pages: write` permission this workflow grants
          # and that you've at least once visited Settings → Pages on
          # the repo (GitHub requires an interactive bootstrap before
          # the API will accept enablement).
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
